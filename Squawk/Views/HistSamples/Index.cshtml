@model IEnumerable<Squawk.Models.HistSample>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Host.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SampleType.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dtSample)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dbValue)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Host.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SampleType.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dtSample)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dbValue)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
